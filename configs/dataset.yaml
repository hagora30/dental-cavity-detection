# configs/dataset.yaml — regenerated by rebuild_splits.py
# Paths are relative to the project root.

path: data/processed
train: train/images
val:   valid/images
test:  test/images

nc: 4
names:
  - Cavity
  - Fillings
  - Impacted Tooth
  - Implant

source:
  platform: Roboflow
  project: dental-cavity-qfnzu
  version: 3
  note: >
    Splits rebuilt from scratch at source-image level to eliminate
    cross-split leakage introduced by Roboflow augmentation hashes.
  seed: 42
